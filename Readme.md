# Go http library 

it is a library that offers a seamless and flexible to send requests with http, with this library, we can set headers, add middlewares, rate limiting and handle retries with backoff mechanism
